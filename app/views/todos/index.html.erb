<p style="color: green"><%= notice %></p>

<% content_for :title, t('todos.index.title') %>

<h1><%= t('todos.index.title') %></h1>

<div id="todos">
  <% @todos.each do |todo| %>
    <%= render todo %>
    <p>
      <%= link_to t('todos.index.show'), todo %>
    </p>
  <% end %>
</div>

<%= link_to t('todos.index.new'), new_todo_path %>
<%= link_to "ホーム画面", root_path %>
